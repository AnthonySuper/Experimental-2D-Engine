general:
  build_dir: build

dependencies:
  pre:
  - sudo apt-get update; sudo apt-get install cmake libsdl2-dev

checkout:
  pre:
    - mkdir build
  post:
    - git submodule sync
    - git submodule update --init

test:
  override:
    - cmake ..
    - make
    - ./catch_test

